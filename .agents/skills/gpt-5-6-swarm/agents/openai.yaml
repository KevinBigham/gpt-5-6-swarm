interface:
  display_name: "GPT-5.6 Swarm"
  short_description: "Fail-closed parallel Codex orchestration"
  default_prompt: "Use $gpt-5-6-swarm to delegate this task across bounded parallel Codex agents and reconcile their results."

policy:
  allow_implicit_invocation: false
