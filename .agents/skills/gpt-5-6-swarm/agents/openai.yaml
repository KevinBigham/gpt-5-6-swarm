interface:
  display_name: "GPT-5.6 Swarm"
  short_description: "Safe parallel Codex agent orchestration"
  default_prompt: "Use $gpt-5-6-swarm to delegate this task across safe parallel Codex agents and reconcile their results."

policy:
  allow_implicit_invocation: false
